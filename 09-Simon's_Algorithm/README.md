# Simon's Algorithm

## Introduction

Simon's Algorithm is a quantum algorithm that finds a **hidden binary string** `s`.

The algorithm is given a function `f(x)` with a hidden property:

```text
f(x) = f(x XOR s)

Simon's algorithm is primarily a foundational quantum algorithm that demonstrates an exponential query advantage and provides important ideas that influenced later algorithms such as Shor's algorithm."
